<?php

class QueuelocksTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('queuelocks')->truncate();

		$queuelocks = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('queuelocks')->insert($queuelocks);
	}

}
