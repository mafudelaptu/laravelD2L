<?php

class MatchhostsTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('matchhosts')->truncate();

		$matchhosts = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('matchhosts')->insert($matchhosts);
	}

}
