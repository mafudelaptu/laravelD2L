<?php

class Matched_usersTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('matched_users')->truncate();

		$matched_users = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('matched_users')->insert($matched_users);
	}

}
